# Emertech — Agriculture dMRV Landing Page

React + Vite. Inline styles (no Tailwind build), fonts via Google Fonts.

## Dev
    npm install
    npm run dev

## Build
    npm run build
    npm run preview

## Deploy
Import the repo on vercel.com — auto-detects Vite (build `npm run build`, output `dist`).
